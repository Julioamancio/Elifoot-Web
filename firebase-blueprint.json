{
  "entities": {
    "GameState": {
      "title": "GameState",
      "description": "The complete state of a user's game save.",
      "type": "object",
      "properties": {
        "teams": {
          "type": "array",
          "description": "List of all teams in the game."
        },
        "matches": {
          "type": "array",
          "description": "List of all matches in the season."
        },
        "currentWeek": {
          "type": "number",
          "description": "The current week of the season."
        },
        "userTeamId": {
          "type": "string",
          "description": "The ID of the team controlled by the user."
        },
        "isGameOver": {
          "type": "boolean",
          "description": "Whether the season is over."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last save."
        }
      },
      "required": ["teams", "matches", "currentWeek", "userTeamId", "isGameOver", "updatedAt"]
    }
  },
  "firestore": {
    "saves/{userId}": {
      "schema": {
        "$ref": "#/entities/GameState"
      },
      "description": "Stores the game save for a specific user."
    }
  }
}
